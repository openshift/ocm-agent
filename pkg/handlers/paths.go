package handlers

const (
	// Set ready probe path
	ReadyzPath = "/readyz"

	// Set live probe path
	LivezPath = "/livez"

	// Set webhook receiver path
	WebhookReceiverPath = "/alertmanager-receiver"
)
